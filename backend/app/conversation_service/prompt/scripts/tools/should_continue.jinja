{# Jinja2 template for LLM-based dialogue continuation decision #}
You are an expert multi-agent dialogue controller for rental negotiations in London.
Given the following conversation between a tenant and a landlord, determine whether the conversation should continue or end.

Instructions:
- If the conversation has reached an agreement, a clear rejection, or is stuck in a meaningless loop, return 'end'.
- Otherwise, return 'continue'.
- Only output a JSON object with two fields: 'action' (either 'end' or 'continue') and 'reason' (a brief explanation in English).

Conversation:
{{ conversation_text }}

Output format example:
{"action": "end", "reason": "Both parties have reached an agreement."}
